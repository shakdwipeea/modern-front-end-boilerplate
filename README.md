# Universal App Boilerplate

* react
* react-router
* redux
* webpack

## Install instruction

```
npm install webpack nodemon -g
npm install
```
Run ` npm start `

Run ` webpack -w` for build and hot reloading
